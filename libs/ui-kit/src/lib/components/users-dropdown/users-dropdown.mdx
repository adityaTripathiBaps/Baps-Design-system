import { Meta } from '@storybook/blocks';
import {
  ResourceTabs,
  DemoCard,
  CodeBlock,
  PropsTable,
  TokensTable,
  KeyboardTable,
} from '@baps/docs-blocks';
import * as UsersDropdownStories from './users-dropdown.stories';

<Meta of={UsersDropdownStories} />

# Users Dropdown

A select-style dropdown built for picking a person.

<ResourceTabs />

The Sampark Portal **Users Dropdown** (Figma node [13197:89016](https://www.figma.com/design/xc0L2xnREMgjyb5XcKyLIz/%F0%9F%9F%A2-Sampark-Portal?node-id=13197-89016)). Like the FormField Sampark dropdowns it presents a **trigger field** — 40px, 4px radius, chevron — and clicking it opens a floating **panel**: a search field over a scrollable list of [`baps-menu-item`](?path=/docs/components-menuitem--docs) rows showing an avatar and a name over a role. Data-driven via `users`; the search filters by title and subtitle client-side. Picking a row sets `value`, emits `valueChange`, and closes. Click-outside and `Esc` also close.

## Usage

<CodeBlock
  filename="assign-karyakar.component.ts"
  language="typescript"
  code={`import { BapsUsersDropdown, type BapsUserOption } from '@org/ui-kit';

@Component({
  imports: [BapsUsersDropdown],
  template: \`
    <baps-users-dropdown
      [users]="users"
      [(value)]="selected"
      placeholder="Select karyakar"
      searchPlaceholder="Search"
      brand="sampark"
    />
  \`,
})
export class AssignComponent {
  users: BapsUserOption[] = [
    { id: 1, name: 'Rajesh Patel', role: 'Regional Admin' },
    { id: 2, name: 'Priya Shah', role: 'Centre Lead' },
  ];
  selected: unknown = null;
}`}
/>

## Examples

<DemoCard
  title="Playground"
  description="Every input on one instance — use the controls panel to try combinations."
  of={UsersDropdownStories.Playground}
/>

<DemoCard
  title="Open Panel"
  description="The panel as it opens: search field above a scrollable list of avatar rows."
  of={UsersDropdownStories.OpenPanel}
/>

<DemoCard
  title="Groups"
  description="Rows grouped under labelled headings, for lists split by region or role."
  of={UsersDropdownStories.Groups}
/>

<DemoCard
  title="Multi Select"
  description="Checkbox rows. The panel stays open so several people can be picked in one pass."
  of={UsersDropdownStories.MultiSelect}
/>

<DemoCard
  title="Radio Select"
  description="Radio rows — single selection, but with the control drawn explicitly."
  of={UsersDropdownStories.RadioSelect}
/>

<DemoCard
  title="States"
  description="Placeholder, selected, disabled, and the empty-search result."
  of={UsersDropdownStories.States}
/>

## API

<PropsTable of={UsersDropdownStories} />

## Theming

The trigger is styled by the component; every row is a `baps-menu-item`, so the list is themed through that component's `--menu-item-*` variables rather than anything local to this dropdown.

<TokensTable
  tokens={[
    {
      token: '--menu-item-bg-selected',
      light: '#f8f7f7',
      dark: '#2b2f32',
      description: 'Row background for both hover and selected — hover previews the selected look.',
    },
    {
      token: '--menu-item-bar',
      light: '#c96868',
      dark: '#c96868',
      description: 'The 3px left accent bar. MyBKY re-points this to its blue.',
    },
    {
      token: '--menu-item-text',
      light: '#151414',
      dark: '#f8fafb',
      description: 'Row title.',
    },
    {
      token: '--menu-item-subtitle',
      light: '#9f9c9c',
      dark: '#b6b6af',
      description: 'Row subtitle — the role line under the name.',
    },
    {
      token: '--menu-item-bar-width',
      light: '3px',
      dark: '3px',
      description: 'Scheme-independent. Set to 0 by baps-listbox, which draws its own bar.',
    },
  ]}
/>

## Accessibility

### Screen Reader

The trigger is a native `<button>` carrying `aria-haspopup="listbox"` and `aria-expanded`, so its open/closed state is announced without any consumer wiring. The panel is `role="listbox"`, and gains `aria-multiselectable` in checkbox mode. Grouped lists wrap each group in `role="group"` labelled by the group's label, and the visible group title is `aria-hidden` so the label is not read twice. The search field is labelled by `searchPlaceholder`.

The component supplies no name for the trigger itself — pass one from the outside (`aria-label`, or a `baps-form-field` label), otherwise the trigger announces only its current value.

**Known gap:** the rows are `baps-menu-item` elements, not `role="option"`. The `role="listbox"` container therefore has no option children and no `aria-activedescendant`, so a screen reader announces the panel as an empty listbox and reads the rows as ordinary content. Treat this as a component-level fix, not something a consumer can work around.

### Keyboard Support

<KeyboardTable
  rows={[
    {
      key: 'Tab',
      action: 'Move to the trigger; when open, move into the search field and then through the rows',
    },
    { key: 'Enter', action: 'Toggle the panel from the trigger, or select the focused row' },
    { key: 'Space', action: 'Toggle the panel from the trigger, or select the focused row' },
    {
      key: 'Esc',
      action: 'Close the panel from anywhere, including while typing in the search field',
    },
  ]}
/>

Arrow-key navigation through the list is not implemented — rows are reached with `Tab`. Combined with the missing `role="option"`, this is the component's weakest area; prefer `baps-select` where a fully keyboard-driven picker is required.

## When Not to Use

| Need | Use instead | Why not this |
| --- | --- | --- |
| Picking a plain value with no avatar or subtitle | `baps-select` | The avatar column and two-line row are the entire reason this component exists |
| A list that stays open in the page, not in a panel | `baps-listbox` | This component is a trigger plus an overlay |
| A list of *actions* rather than values | `baps-menu-item` in your own container, or `baps-split-button` | Selecting here means "this is now the value", not "do this" |
| Showing who is already assigned, read-only | `baps-avatargroup` | |
| Server-side search over thousands of users | Not this component | `filtered` is a synchronous `Array.filter` over the whole `users` input on every keystroke — no debounce, no virtualization, no lazy loading. `searchChange` emits the query so a consumer *could* refetch, but the internal filter still runs over whatever `users` currently holds |
| Keyboard-driven option navigation | Not this component | Arrow-key navigation through the list is explicitly out of scope |
